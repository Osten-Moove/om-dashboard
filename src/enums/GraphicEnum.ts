export enum GraphicMetrics {
  PERCENTAGE = 'PERCENTAGE',
  TOTAL = 'TOTAL',
}

export enum GraphicType {
  LINE_CHART = 'LINE_CHART',
  BAR_CHART = 'BAR_CHART',
  PIE_CHART = 'PIE_CHART',
  TABLE = 'TABLE',
}
